describe('github functions work as expected', () => {
  test.skip('')
})

describe('github functions integrate as expected', () => {
  test('getCommitsSinceLastRelease', async () => {
    expect(typeof '').toBe('string')
  })
})
